import React from 'react';
import Button from '../Button';

const AuthNav = () => (
  <div className="right menu">
    <div className="item">
      <Button text="Home" />
    </div>
  </div>
);

export default AuthNav;
